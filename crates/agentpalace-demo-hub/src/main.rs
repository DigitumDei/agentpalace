//! Local demo gateway entry point. Deployment wiring supplies configuration and
//! verified Google OIDC claims; no credentials are embedded here.

fn main() {
    eprintln!("agentpalace-demo-hub: configure the gateway through the hosting example; no default listener is enabled");
}
